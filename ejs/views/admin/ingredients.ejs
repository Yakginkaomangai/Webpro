<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</head>
<body>
    <div class="container mt-4">
        <% if (isAdmin) { %>
            <%- include('../partials/navbar') %>

            <div class="container mt-4 mb-5">    
                <h1 class="my-4 text-center">Admin Dashboard</h1>
            
                <div class="d-flex justify-content-between">
                    <a href="/admin/ingredients/add" class="btn btn-success mb-3">เพิ่มวัตถุดิบใหม่</a>
                    <div>
                        <a href="/admin" class="btn btn-primary mb-3">เมนูทั้งหมด</a>
                        <a href="/admin/ingredients" class="btn btn-secondary mb-3">วัตถุดิบทั้งหมด</a>
                    </div>
                </div>
            
                <h2>รายการเมนู</h2>
                <div class="table-responsive">
                    <table class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th>รูปวัตถุดิบ</th>
                                <th>ชื่อวัตถุดิบ</th>
                                <th>ชื่อวัตถุดิบภาษาไทย</th>
                                <th>ราคา</th>
                                <th>ประเภท</th>
                                <th>จัดการ</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% ingredients.forEach(ingredients => { %>
                                <tr>
                                    <td><img src="<%= ingredients.img %>" class="img-fluid w-100" style="height: 150px; object-fit: cover; border-radius: 15px;"></td>
                                    <td><%= ingredients.name %></td>
                                    <td><%= ingredients.thname %></td>
                                    <td><%= ingredients.price %></td>
                                    <td><%= ingredients.type %></td>
                                    <td>
                                        <a href="/admin/ingredients/edit/<%= ingredients.ingre_id %>" class="btn btn-warning btn-sm">แก้ไข</a>
                                        <form action="/admin/ingredients/delete/<%= ingredients.ingre_id %>" method="POST" style="display:inline;">
                                            <button type="submit" class="btn btn-danger btn-sm">ลบ</button>
                                        </form>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        <% } else { %>
            <p class="text-danger">คุณไม่มีสิทธิ์เข้าถึงหน้านี้</p>
        <% } %>
    </div>
</div>

    <%- include('../partials/footer') %>
</body>
</html>
