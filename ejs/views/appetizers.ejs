<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Appetizers</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Text&family=Roboto+Slab:wght@100..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>


</head>
<body class="d-flex flex-column min-vh-100">

    <!-- Navbar -->
    <%- include('partials/navbar') %>

    <div class="container mt-4 mb-5">
        <div class="container mt-4">
            <div class="row mt-3">
                <div class="col-md-9 d-flex justify-content-between gap-3">
                    <div class="flex-grow-1 bg-light p-3 text-center rounded-3"><a class="nav-link" href="/allsandwich">SANDWICH</a></div>
                    <div class="flex-grow-1 bg-light p-3 text-center rounded-3"><a class="nav-link" href="/comboset">COMBO SET</a></div>
                    <div class="flex-grow-1 bg-light p-3 text-center rounded-3"><a class="nav-link" href="/appetizers">APPETIZER</a></div>
                    <div class="flex-grow-1 bg-light p-3 text-center rounded-3"><a class="nav-link" href="/drinks">DRINK</a></div>
                </div>
            </div>
        </div>
            
        <!-- เมนูสินค้า -->
        <div class="row mt-5">
            <div class="col-md-9">
                <h3>APPETIZERS</h3>
                <div class="row mt-3 mb-5">
                    <% appetizers.forEach(appetizers => { %>
                        <div class="col-md-4">
                            <div class="menu-item text-start mt-2">
                                <img src="<%= appetizers.image %>" class="img-fluid w-100" style="height: 180px; object-fit: cover;">
                                <p class="text-success fw-bold small mt-4"><%= appetizers.price %> THB</p>
                                <p class="fw-bold"><%= appetizers.name %></p> 
                                <div class="hover-content">
                                    <input type="number" class="form-control qty-input" value="1" min="1">
                                    <button class="btn btn-sm btn-success mt-2 w-100">Add to Cart</button>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>
        </div>
    </div>  

    <div class="container mb-5">
        <!-- ตะกร้าสินค้า -->
        <div class="cart-box">
            <h4>ตะกร้าสินค้า</h4>
            <p>สินค้าในตะกร้า...</p>
            <div class="cart-footer">
                <button class="btn btn-success w-100">
                    <a href="/checkout" class="nav-link">Checkout</a>
                </button>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>
</body>
</html>
