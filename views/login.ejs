<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<body class="d-flex flex-column min-vh-100">
    <%- include('partials/navbar') %>

    <div class="container d-flex justify-content-center align-items-center mt-3 mb-5">
        <div class="row align-items-center gap-3">
            <!-- ฟอร์มล็อกอิน -->
            <div class="col-md-5 d-flex justify-content-end">
                <div class="login-box p-3 shadow rounded bg-white">
                    <h2>ยินดีต้อนรับ !</h2>
                    <p>โปรดเข้าสู่ระบบเพื่อดำเนินการต่อ</p>
                    <% if (error) { %>
                        <div class="alert alert-danger">
                            <%= error %>
                        </div>
                    <% } %>
                    <form action="/login" method="POST">
                        <div class="mb-2">
                            <label class="form-label">อีเมล</label>
                            <input type="email" name="email" class="form-control" placeholder="Enter your email" required>
                        </div>
                        <div class="mb-2">
                            <label class="form-label">รหัสผ่าน</label>
                            <input type="password" name="password" class="form-control" placeholder="********" required>
                        </div>
                        <button type="submit" class="btn btn-success w-100 mt-2">เข้าสู่ระบบ</button>
                        <p class="text-center mt-2">คุณยังไม่มีสมาชิก? <a href="/register" class="text-success">สมัครสมาชิกฟรี!</a></p>
                    </form>
                </div>
            </div>

            <!-- รูปภาพ -->
            <div class="col-md-5 d-flex justify-content-start">
                <div class="promo-container">
                    <img src="/img/promote.png" alt="Sandwich Image" class="img-fluid rounded shadow">
                </div>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>
</body>
</html>
